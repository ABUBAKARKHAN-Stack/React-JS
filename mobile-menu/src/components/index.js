import Header from './header/Header'; // Check if './Header' works if it’s in the same folder
import HeroSection from './HeroSection'; // Check if the path is accurate
import Footer from './footer/Footer'; // Ensure the folder and file names are correct


export { Header, HeroSection, Footer }